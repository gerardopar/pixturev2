import React from 'react';

const heroTitle = () => (
    <div className="hero__title--container">
        <div className="hero__title--wrap">
            <h1 className="hero__title">High quality free stock photos <br /> shared by talented users</h1>
        </div>
    </div>
);

export default heroTitle;