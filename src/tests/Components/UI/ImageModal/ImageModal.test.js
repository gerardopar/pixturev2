import React from 'react';
import { shallow } from 'enzyme';
import ImageModal from '../../../../components/UI/ImageModal/ImageModal';

describe('renders <ImageModal /> component with correct props', () => {
    test('should render modal with props', () => {
        
    });
});
